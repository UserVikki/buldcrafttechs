# Server Configuration
server.port=8080

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=buildcrafttechs@gmail.com
spring.mail.password=imiuhcbrmuefepwi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Recipient email for contact form submissions (Admin email)
app.mail.to=BuildCraftTechs@gmail.com

# Set to true to enable email sending (requires valid SMTP credentials above)
app.mail.enabled=true

